import { StyleSheet } from 'react-native';

export const styleTimeAgo = StyleSheet.create({
    main: {
        fontSize: 12,
        color: 'silver',
        marginRight: 10,
        alignSelf: 'center',
        fontStyle: 'italic'
    }
})